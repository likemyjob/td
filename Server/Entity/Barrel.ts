export class Barrel {
    id: number;
    radius: number;
    world_id: number;
    recipe_id: number;
    position: any;

    // update() {
    //     this.em.players.forEach((entity: Player) => {
    //         entity.socket.emit('transfer', this.getData());
    //     });
    // }
}
